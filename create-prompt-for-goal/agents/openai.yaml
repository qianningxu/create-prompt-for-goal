interface:
  display_name: "Create Prompt for Goal"
  short_description: "Pressure-test all six goal fields"
  default_prompt: "Use $create-prompt-for-goal to ask questions until all six Goal fields pass, then output only the final `/goal` prompt, using section headings if it is over 50 words."
