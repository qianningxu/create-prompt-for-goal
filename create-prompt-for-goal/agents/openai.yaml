interface:
  display_name: "Create Prompt for Goal"
  short_description: "Pressure-test all six goal fields"
  default_prompt: "Use $create-prompt-for-goal to ask questions until the Goal fields pass, then output only a heading-structured `/goal` prompt; omit Stop and report only if I explicitly insist."
